<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" Padding="10"
             x:Class="PCcamAdmin.Views.Details">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <CarouselView x:Name="Photos" ItemsSource="{Binding imgs}" VerticalOptions="Start" HorizontalOptions="FillAndExpand"
                  >
                    <CarouselView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Frame IsClippedToBounds="True">
                                    <Image Source="{Binding imgurl}" HorizontalOptions="FillAndExpand"/>
                                </Frame>
                            </ViewCell>
                        </DataTemplate>
                    </CarouselView.ItemTemplate>
                </CarouselView>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>